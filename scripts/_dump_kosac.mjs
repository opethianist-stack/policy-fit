const u = 'https://www.kosac.re.kr/menus/274/bns';
const ua = { headers: { 'user-agent': 'Mozilla/5.0' } };
const h = await (await fetch(u, ua)).text();
const i = h.indexOf('/menus/274/bns/PBANC_');
console.log('=== LIST ROW ===');
console.log(h.slice(Math.max(0, i - 1500), i + 2500).replace(/\s+/g, ' '));
const j = h.lastIndexOf('/menus/274/bns/PBANC_');
console.log('=== LIST TAIL (pagination) ===');
console.log(h.slice(j, j + 3500).replace(/\s+/g, ' '));
const p2 = await (await fetch(u + '?page=2', ua)).text();
console.log('=== PAGE2 first id ===', (p2.match(/PBANC_\d+/) || [''])[0]);
const first = h.match(/\/menus\/274\/bns\/PBANC_\d+/)[0];
const d = await (await fetch(new URL(first, u), ua)).text();
const k = d.search(/접수\s*기간|신청\s*기간|공고\s*기간/);
console.log('=== DETAIL', first, '===');
console.log(d.slice(Math.max(0, k - 2500), k + 2000).replace(/\s+/g, ' '));
